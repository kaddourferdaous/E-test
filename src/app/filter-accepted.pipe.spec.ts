import { FilterAcceptedPipe } from './filter-accepted.pipe';

describe('FilterAcceptedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAcceptedPipe();
    expect(pipe).toBeTruthy();
  });
});
